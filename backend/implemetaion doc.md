1. Full API implementation (/home/claude/budget-planner-api/) — 12 Express router modules covering every feature derived directly from mockState.ts, all 8 API service files, and mock/data.json:
ModuleKey endpointsAuthregister, login, refresh, logout, me, profile, forgot/reset password, PIN set/verifyDashboardwealth snapshot, 12-month summaries, SMS pending countExpensesCRUD transactions, anomaly detection (matches frontend logic exactly), bulk delete/recategorize, disputeCategoriesCRUD with live current-month spend, over-limit flagBudgetmonthly plans, item allocations, zero-based ↔ flexible mode toggleGoalsCRUD + progress update, auto daily_nudge calculationLoansBorrowed/Lent tracker, payment recording with wealth impactAccountsBank accounts / walletsCardsCredit cards (limit, due date, min due) + debit cardsNotificationsInbox, read/mark-all, auto-created on anomalyReportsstats (bar chart), yearly insights, recurring bills, date-range exportSettingspreferences, full data backup JSON, data clear